import React from 'react';
import { FilterElement } from './FilterElement';

export const ShowFilterView = ()=>{
  
  return(
    <div id="showFilterView">
      <div id="scrollFilterView" >
        <FilterElement id="1" />
      </div>
    </div>
  )
}